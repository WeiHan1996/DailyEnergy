name: Fork secret bracket fixture
on: pull_request
permissions:
  contents: read
jobs:
  fixture:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - run: echo "${{ secrets['PRODUCTION_TOKEN'] }}"
