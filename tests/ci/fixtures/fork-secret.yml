name: known-fail-fork-secret
on: pull_request_target
permissions:
  contents: read
jobs:
  static:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - run: echo "${{ secrets.PRODUCTION_TOKEN }}"
