name: Fork secret wrapped fixture
on: pull_request
permissions:
  contents: read
jobs:
  fixture:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - env:
          SECRET_CONTEXT: ${{ toJSON(secrets) }}
        run: echo "fixture"
