name: known-fail-excessive-permission
on: pull_request
permissions:
  contents: write
  id-token: write
jobs:
  static:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - run: node --version
