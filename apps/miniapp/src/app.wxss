page {
  min-height: 100%;
  background: #f6f3ee;
  color: #24302b;
  font-family:
    -apple-system, BlinkMacSystemFont, "Helvetica Neue", "PingFang SC",
    sans-serif;
}

button {
  border-radius: 999rpx;
  font-weight: 600;
}

button::after {
  border: none;
}
